//
//  RestaurantCategoriesSection.swift
//  MealMate
//
//  Created by HASSEN on 3/12/2022.
//

import SwiftUI
/*
struct CourseCategoriesSection: View {
    @State private var courseCategories = exploreCourseCategories
    
    var body: some View {
        VStack(spacing: 15) {
            ListSectionHeader(title: "Course Categories", doesHaveAButton: true, buttonText: "See All")
                .frame(width: ScreenSize.screenWidth-32)
            
            categories
        }
    }
    
    var categories: some View {
        VStack(spacing: 20) {
            ListDivider()
            
            ForEach(courseCategories) { courseCategory in
                ExploreCourseCategoryItem(gradientIndex: courseCategories.firstIndex(of: courseCategory) ?? 0, exploreCourseCategory: courseCategory)
                ListDivider()
            }
        }
    }
}
*/
